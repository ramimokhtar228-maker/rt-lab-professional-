# Supabase

Run migrations in filename order. The production migration adds specimen/result-version/analyzer/reflex/result-event/Test Master tables. The clinical engine migration adds reference, critical, calculation, validation, delta, approval, report-version and audit tables.
